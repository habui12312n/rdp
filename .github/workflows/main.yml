name: CI

on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Tải playit.gg Agent
      shell: powershell
      run: |
        Invoke-WebRequest -Uri "https://github.com/playit-cloud/playit-agent/releases/download/v0.15.0/playit-windows-x86_64.exe" -OutFile "playit.exe"

    - name: Di chuyển file thực thi playit
      shell: powershell
      run: |
        New-Item -ItemType Directory -Path ".\playit" -Force
        Move-Item -Path "playit.exe" -Destination ".\playit\playit.exe" -Force

    - name: Kiểm tra playit.gg Agent
      shell: powershell
      run: |
        Write-Output "Kiểm tra phiên bản playit.gg agent..."
        .\playit\playit.exe --version
        .\playit\playit.exe --help

    - name: Xác thực Agent bằng Secret Key
      shell: powershell
      env:
        PLAYIT_SECRET_KEY: ${{ secrets.PLAYIT_SECRET_KEY }}
      run: |
        .\playit\playit.exe --secret $env:PLAYIT_SECRET_KEY

    - name: Khởi động Tunnel (nền, không block)
      shell: powershell
      run: |
        Start-Process -FilePath ".\playit\playit.exe" -ArgumentList "run" -WindowStyle Hidden
        Write-Output "✅ Tunnel đang chạy nền"

    - name: Bật Terminal Services
      shell: powershell
      run: Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0

    - name: Bật quy tắc tường lửa Remote Desktop
      shell: powershell
      run: Enable-NetFirewallRule -DisplayGroup "Remote Desktop"

    - name: Cấu hình bảo mật RDP
      shell: powershell
      run: Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 1

    - name: Đặt mật khẩu người dùng cục bộ
      shell: powershell
      run: |
        $password = ConvertTo-SecureString "P@ssw0rd!" -AsPlainText -Force
        Set-LocalUser -Name "runneradmin" -Password $password

    - name: Giữ phiên làm việc trong 24 giờ
      shell: powershell
      run: |
        Write-Output "🕒 Đang giữ phiên làm việc trong 24 giờ (86400 giây)..."
        Start-Sleep -Seconds 86400
